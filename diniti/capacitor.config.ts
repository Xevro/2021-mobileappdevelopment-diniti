import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.louis.diniti',
  appName: 'Diniti',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
